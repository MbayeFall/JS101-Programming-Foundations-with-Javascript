// By using the string method endsWith('char'). Char representing the character we want to check if it's the last character of the string.

let str1 = "Come over here!"; // true
let str2 = "What's up, Doc?"; // false

str1.endsWith('!');
str2.endsWith('!')